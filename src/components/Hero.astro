---
import {
  ArrowRight,
  BriefcaseBusiness,
  CodeXml,
  Download,
  Layers,
  Mail,
  MapPin,
  ShieldCheck,
  Sparkles
} from '@lucide/astro';
import { profile } from '@/data/profile';
import TechIcon from '@/components/TechIcon.astro';

// Structured 7-Stage DevOps Delivery Pipeline Flow
// 01 Cloud -> 02 IaC -> 03 Containers -> 04 Platform -> 05 CI/CD -> 06 GitOps -> 07 Observability
const pipelineStages = [
  {
    stage: '01',
    name: 'Cloud Infrastructure',
    shortName: 'Cloud',
    tag: 'FOUNDATION',
    domain: 'Public Cloud & VPC',
    accent: 'cyan',
    accentColor: 'var(--primary)',
    accentGlow: 'var(--primary-glow)',
    accentBorder: 'var(--border)',
    technologies: [
      { id: 'aws', name: 'AWS' },
      { id: 'azure', name: 'Azure' }
    ],
    highlight: 'Multi-AZ VPCs, EKS, EC2, S3, RDS & IAM security',
    delay: '0s',
    x: 95,
    y: 75
  },
  {
    stage: '02',
    name: 'Infrastructure as Code',
    shortName: 'IaC',
    tag: 'PROVISIONING',
    domain: 'Declarative State',
    accent: 'violet',
    accentColor: 'var(--secondary)',
    accentGlow: 'var(--secondary-glow)',
    accentBorder: 'var(--border)',
    technologies: [
      { id: 'terraform', name: 'Terraform' },
      { id: 'ansible', name: 'Ansible' }
    ],
    highlight: 'Automated cloud provisioning, modules & state locks',
    delay: '1.4s',
    x: 320,
    y: 75
  },
  {
    stage: '03',
    name: 'Containers',
    shortName: 'Containers',
    tag: 'PACKAGING',
    domain: 'Immutable Runtime',
    accent: 'cyan',
    accentColor: 'var(--primary)',
    accentGlow: 'var(--primary-glow)',
    accentBorder: 'var(--border)',
    technologies: [
      { id: 'docker', name: 'Docker' }
    ],
    highlight: 'Multi-stage OCI builds & minimal attack surfaces',
    delay: '2.8s',
    x: 545,
    y: 75
  },
  {
    stage: '04',
    name: 'Orchestration',
    shortName: 'Platform',
    tag: 'PLATFORM',
    domain: 'Kubernetes Platform',
    accent: 'cyan',
    accentColor: 'var(--primary)',
    accentGlow: 'var(--primary-glow)',
    accentBorder: 'var(--border)',
    technologies: [
      { id: 'kubernetes', name: 'Kubernetes' },
      { id: 'helm', name: 'Helm' }
    ],
    highlight: 'EKS/AKS clusters, 12+ microservices & ALB ingress',
    delay: '4.2s',
    x: 545,
    y: 255
  },
  {
    stage: '05',
    name: 'CI/CD Automation',
    shortName: 'CI / CD',
    tag: 'DELIVERY',
    domain: 'Automated Pipelines',
    accent: 'amber',
    accentColor: 'var(--primary)',
    accentGlow: 'var(--primary-glow)',
    accentBorder: 'var(--border)',
    technologies: [
      { id: 'githubactions', name: 'GitHub Actions' },
      { id: 'jenkins', name: 'Jenkins' }
    ],
    highlight: 'Automated test, SonarQube quality & Trivy security gates',
    delay: '5.6s',
    x: 320,
    y: 255
  },
  {
    stage: '06',
    name: 'GitOps Delivery',
    shortName: 'GitOps',
    tag: 'GITOPS',
    domain: 'Declarative Sync',
    accent: 'mint',
    accentColor: 'var(--success)',
    accentGlow: 'var(--success-glow)',
    accentBorder: 'var(--border)',
    technologies: [
      { id: 'argocd', name: 'Argo CD' }
    ],
    highlight: 'Zero-downtime releases & automated cluster sync',
    delay: '7.0s',
    x: 95,
    y: 255
  },
  {
    stage: '07',
    name: 'Observability & Telemetry',
    shortName: 'Telemetry',
    tag: 'OBSERVE',
    domain: 'APM, Logs & Synthetics',
    accent: 'mint',
    accentColor: 'var(--success)',
    accentGlow: 'var(--success-glow)',
    accentBorder: 'var(--border)',
    technologies: [
      { id: 'prometheus', name: 'Prometheus' },
      { id: 'grafana', name: 'Grafana' },
      { id: 'dynatrace', name: 'Dynatrace' },
      { id: 'elastic', name: 'Elastic Stack' }
    ],
    highlight: 'Dynatrace APM, Catchpoint synthetics & fast RCA',
    delay: '8.4s',
    x: 320,
    y: 435
  }
];

// Continuous SVG Pipeline Circuit Path for Desktop (640x480) - terminates cleanly inside Stage 07 (x: 320, y: 435)
const desktopCircuitPath = 'M 95 75 L 320 75 L 545 75 C 625 75, 625 255, 545 255 L 320 255 L 95 255 C 15 255, 15 435, 95 435 L 320 435';
---

<section class="relative overflow-hidden pt-6 sm:pt-10 lg:pt-14 pb-12 sm:pb-16 lg:pb-24" id="top">

  <!-- Layered Ambient Depth Field -->
  <div class="pointer-events-none absolute inset-0 overflow-hidden" aria-hidden="true">
    <!-- Primary atmospheric orb - top left -->
    <div class="hero-orb hero-orb--cyan absolute -top-40 -left-16 h-[800px] w-[800px] rounded-full"></div>
    <!-- Secondary orb - right center -->
    <div class="hero-orb hero-orb--violet absolute top-1/4 -right-24 h-[700px] w-[700px] rounded-full"></div>
    <!-- Tertiary orb - bottom -->
    <div class="hero-orb hero-orb--mint absolute -bottom-28 left-1/3 h-[600px] w-[600px] rounded-full"></div>
    <!-- Engineering grid overlay -->
    <div class="absolute inset-0 hero-grid-pattern opacity-[0.055] dark:opacity-[0.14]"></div>
    <!-- Radial vignette top -->
    <div class="absolute inset-0 bg-[radial-gradient(ellipse_70%_55%_at_50%_0%,rgba(212,169,78,0.04)_0%,transparent_65%)] dark:bg-[radial-gradient(ellipse_70%_55%_at_50%_0%,rgba(212,169,78,0.09)_0%,transparent_65%)]"></div>
  </div>

  <div class="relative mx-auto max-w-7xl px-4 sm:px-6 lg:px-8 w-full min-w-0">

    <!-- Main Hero Layout: Left Column (Identity) + Right Column (Pipeline) -->
    <div class="grid items-center gap-10 lg:grid-cols-[1.05fr_1.15fr] lg:gap-12 xl:gap-14">

      <!-- ========================================================================= -->
      <!-- LEFT COLUMN: Editorial Identity, Profile Anchor & High-Contrast CTAs      -->
      <!-- ========================================================================= -->
      <div class="space-y-6 sm:space-y-7">

        <!-- 1. Live Availability & Location Status Badge -->
        <div class="hero-animate-in flex flex-wrap items-center gap-2.5" style="animation-delay: 0ms;">
          <span class="inline-flex items-center gap-2 rounded-full border border-mint/40 bg-mint/[0.08] px-3.5 py-1 font-mono text-[11px] font-semibold text-mint">
            <span class="relative flex h-2 w-2">
              <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-mint opacity-75"></span>
              <span class="relative inline-flex rounded-full h-2 w-2 bg-mint"></span>
            </span>
            <span>Available for DevOps, Cloud &amp; SRE Roles</span>
          </span>

          <span class="inline-flex items-center gap-1.5 font-mono text-[11px] text-slate-500 dark:text-slate-400">
            <MapPin size={13} class="text-cyan flex-shrink-0" />
            <span>{profile.location}</span>
          </span>
        </div>

        <!-- 2. Integrated Human Profile Anchor + Typographic Name -->
        <div class="hero-animate-in space-y-4" style="animation-delay: 80ms;">

          <div class="flex items-start gap-4 sm:gap-5">
            <!-- Engineer Profile Portrait with Layered Glow Halo -->
            <div class="relative flex-shrink-0 group hero-profile-anchor">
              <!-- Animated ambient halo (always visible, subtle breathing) -->
              <div class="hero-profile-halo absolute -inset-4 rounded-[26px] blur-2xl"></div>
              <!-- Primary gradient halo border -->
              <div class="absolute -inset-[2.5px] rounded-2xl bg-gradient-to-tr from-cyan via-violet to-mint opacity-70 dark:opacity-85 blur-[3px] transition-all duration-500 group-hover:opacity-100 group-hover:blur-[5px]"></div>
              <!-- Inner border ring -->
              <div class="absolute -inset-[1px] rounded-2xl bg-gradient-to-tr from-cyan/80 via-violet/60 to-mint/80 opacity-60 dark:opacity-75"></div>
              <!-- Photo container -->
              <div class="relative h-[110px] w-[110px] sm:h-[130px] sm:w-[130px] rounded-2xl bg-slate-100 dark:bg-panel shadow-2xl overflow-hidden">
                <img
                  src={profile.photo}
                  alt={profile.name}
                  class="h-full w-full object-cover object-center transition-transform duration-700 group-hover:scale-[1.08]"
                  loading="eager"
                  width="130"
                  height="130"
                />
                <!-- Subtle inner luminance overlay -->
                <div class="absolute inset-0 bg-gradient-to-t from-slate-900/10 to-transparent pointer-events-none"></div>
              </div>
              <!-- Experience badge -->
              <div class="absolute -bottom-2.5 -right-1.5 rounded-full border border-slate-200/90 dark:border-white/20 bg-white/98 dark:bg-slate-900/95 px-2 py-0.5 shadow-lg flex items-center gap-1.5 backdrop-blur-sm">
                <span class="h-1.5 w-1.5 rounded-full bg-mint animate-pulse" aria-hidden="true"></span>
                <span class="font-mono text-[8.5px] font-bold text-slate-800 dark:text-slate-200">5+ YRS</span>
              </div>
            </div>

            <!-- Typographic Name & Eyebrow Category -->
            <div class="space-y-1 min-w-0 flex-1">
              <span class="block font-mono text-[11px] sm:text-xs uppercase tracking-[0.22em] text-cyan font-bold">
                Engineering Portfolio
              </span>
              <h1 class="text-3xl sm:text-4xl lg:text-5xl xl:text-[3.4rem] font-black tracking-tight font-display leading-[1.05] text-slate-900 dark:text-white" aria-label="Chandu Venna">
                <span class="inline sm:block">Chandu</span>{' '}
                <span class="inline-flex items-baseline min-w-[5.8ch]" aria-hidden="true">
                  <span id="hero-venna-typing" class="venna-typing-root">
                    <span class="venna-char venna-char-1">V</span>
                    <span class="venna-char venna-char-2">E</span>
                    <span class="venna-char venna-char-3">N</span>
                    <span class="venna-char venna-char-4">N</span>
                    <span class="venna-char venna-char-5">A</span>
                  </span>
                  <span class="hero-name-caret" aria-hidden="true"></span>
                </span>
                <span class="sr-only">Venna</span>
              </h1>
            </div>
          </div>

          <!-- Professional Designation Rule (Permanently static, no animation, zero rotation) -->
          <div class="flex items-center gap-3 pt-1">
            <div class="h-px w-14 rounded-full bg-gradient-to-r from-cyan to-violet"></div>
            <p class="font-display text-lg sm:text-xl lg:text-2xl font-bold tracking-tight text-slate-800 dark:text-slate-100">
              DevOps &amp; Cloud Engineer
            </p>
          </div>
        </div>

        <!-- 3. Value Proposition Statement -->
        <div class="hero-animate-in" style="animation-delay: 150ms;">
          <p class="text-base sm:text-[1.05rem] text-slate-600 dark:text-slate-300 leading-relaxed max-w-xl">
            Building reliable cloud infrastructure, Kubernetes platforms, and automated delivery systems across AWS and Azure.
          </p>
        </div>

        <!-- 4. Resume-Verified Proof Points -->
        <div class="hero-animate-in flex flex-wrap items-center gap-2 pt-1 font-mono text-xs" style="animation-delay: 200ms;">
          <span class="hero-proof-pill">
            <ShieldCheck size={13} class="text-cyan" />
            <span>5+ Yrs Production Ops</span>
          </span>
          <span class="hero-proof-pill">
            <Layers size={13} class="text-violet" />
            <span>Multi-AZ K8s &amp; IaC</span>
          </span>
          <span class="hero-proof-pill">
            <Sparkles size={13} class="text-mint" />
            <span>60% Faster Deployments</span>
          </span>
        </div>

        <!-- 5. High-Contrast Primary & Secondary CTAs -->
        <div class="hero-animate-in pt-1 flex flex-wrap items-center gap-3.5" style="animation-delay: 240ms;">
          <!-- Primary CTA -->
          <a
            href="/projects"
            class="hero-cta-primary group inline-flex items-center gap-2.5"
          >
            <span>Explore Case Studies</span>
            <ArrowRight size={16} class="transition-transform duration-200 group-hover:translate-x-1" />
          </a>

          <!-- Secondary CTA -->
          <a
            href={profile.resume}
            target="_blank"
            rel="noopener noreferrer"
            class="hero-cta-secondary inline-flex items-center gap-2"
          >
            <Download size={16} />
            <span>Resume</span>
          </a>
        </div>

        <!-- 6. Social & Direct Contact Channels -->
        <div class="hero-animate-in pt-1 flex items-center gap-4 text-xs font-medium text-slate-600 dark:text-slate-400" style="animation-delay: 290ms;">
          <a
            href={profile.github}
            target="_blank"
            rel="noopener noreferrer"
            class="flex items-center gap-1.5 hover:text-slate-950 dark:hover:text-white transition-colors duration-200"
            aria-label="GitHub Profile"
          >
            <CodeXml size={16} class="text-slate-500 dark:text-slate-400" />
            <span>GitHub</span>
          </a>
          <span class="text-slate-200 dark:text-slate-700">/</span>
          <a
            href={profile.linkedin}
            target="_blank"
            rel="noopener noreferrer"
            class="flex items-center gap-1.5 hover:text-violet transition-colors duration-200"
            aria-label="LinkedIn Profile"
          >
            <BriefcaseBusiness size={16} class="text-slate-500 dark:text-slate-400" />
            <span>LinkedIn</span>
          </a>
          <span class="text-slate-200 dark:text-slate-700">/</span>
          <a
            href={`mailto:${profile.email}`}
            class="flex items-center gap-1.5 hover:text-cyan transition-colors duration-200"
            aria-label="Send Email"
          >
            <Mail size={16} class="text-slate-500 dark:text-slate-400" />
            <span>Email</span>
          </a>
        </div>

      </div>

      <!-- ========================================================================= -->
      <!-- RIGHT COLUMN: Animated DevOps Delivery Pipeline (Desktop 1024px+)         -->
      <!-- ========================================================================= -->
      <div class="hero-animate-in relative hidden lg:block" style="animation-delay: 150ms;">

        <!-- Pipeline Glass Console Window -->
        <div
          class="pipeline-console-panel relative w-full rounded-3xl overflow-hidden"
          id="devops-pipeline-console"
          role="region"
          aria-label="Interactive DevOps Delivery Pipeline Animation"
        >
          <!-- Background depth layers inside the console -->
          <div class="absolute inset-0 rounded-3xl overflow-hidden pointer-events-none" aria-hidden="true">
            <div class="absolute inset-0 bg-gradient-to-br from-slate-50/97 via-white/92 to-slate-50/97 dark:from-panel/97 dark:via-ink/92 dark:to-panel/97"></div>
            <!-- Top radial glow inside panel -->
            <div class="absolute -top-20 left-1/2 -translate-x-1/2 w-[600px] h-[400px] rounded-full bg-cyan/[0.03] dark:bg-cyan/[0.05] blur-3xl"></div>
            <!-- Bottom radial glow inside panel -->
            <div class="absolute -bottom-20 left-1/2 -translate-x-1/2 w-[500px] h-[300px] rounded-full bg-mint/[0.025] dark:bg-mint/[0.04] blur-3xl"></div>
            <!-- Micro dot grid inside console -->
            <div class="absolute inset-0 hero-dot-grid opacity-[0.035] dark:opacity-[0.07]"></div>
          </div>

          <!-- Console inner border with gradient shimmer -->
          <div class="absolute inset-0 rounded-3xl pointer-events-none" aria-hidden="true">
            <div class="absolute inset-0 rounded-3xl ring-1 ring-slate-200/80 dark:ring-white/[0.06]"></div>
            <div class="absolute inset-0 rounded-3xl bg-gradient-to-b from-white/50 to-transparent dark:from-white/[0.03] dark:to-transparent opacity-60"></div>
          </div>

          <div class="relative z-10 p-5 sm:p-6">
            <!-- Top Console Header -->
            <div class="flex items-center justify-between pb-4 border-b border-slate-200/70 dark:border-white/[0.06] mb-3">
              <div class="flex items-center gap-2.5">
                <!-- Traffic lights -->
                <div class="flex items-center gap-1.5">
                  <span class="h-2.5 w-2.5 rounded-full bg-danger/80 dark:bg-danger"></span>
                  <span class="h-2.5 w-2.5 rounded-full bg-warning/80 dark:bg-warning"></span>
                  <span class="h-2.5 w-2.5 rounded-full bg-success/80 dark:bg-success"></span>
                </div>
                <span class="font-mono text-[10px] uppercase tracking-widest text-slate-500 dark:text-slate-400 ml-1">
                  devops-pipeline.sh
                </span>
              </div>

              <div class="flex items-center gap-2 font-mono text-[10px] text-slate-500 dark:text-slate-400">
                <span class="inline-flex items-center gap-1 rounded-md bg-cyan/10 dark:bg-cyan/[0.12] px-2 py-0.5 text-cyan font-bold border border-cyan/20">
                  7 STAGES
                </span>
                <span class="hidden xl:inline">
                  <span class="inline-flex items-center gap-1 text-mint font-semibold">
                    <span class="h-1.5 w-1.5 rounded-full bg-mint animate-pulse"></span>
                    60% Faster Velocity
                  </span>
                </span>
              </div>
            </div>

            <!-- =================================================================== -->
            <!-- DESKTOP PIPELINE CANVAS: SVG Track + Sequential Traveling Pulses   -->
            <!-- =================================================================== -->
            <div class="relative w-full h-[470px] select-none">

              <!-- SVG Circuit Tracks & Animated Data Streams -->
              <svg
                class="absolute inset-0 h-full w-full pointer-events-none overflow-visible z-0"
                viewBox="0 0 640 480"
                fill="none"
                xmlns="http://www.w3.org/2000/svg"
                aria-hidden="true"
              >
                <defs>
                  <!-- Main signal glow filter -->
                  <filter id="signal-glow" x="-100%" y="-100%" width="300%" height="300%">
                    <feGaussianBlur stdDeviation="5" result="blur1"/>
                    <feGaussianBlur stdDeviation="2.5" result="blur2"/>
                    <feMerge>
                      <feMergeNode in="blur1"/>
                      <feMergeNode in="blur2"/>
                      <feMergeNode in="SourceGraphic"/>
                    </feMerge>
                  </filter>

                  <!-- Node glow filter -->
                  <filter id="node-glow" x="-60%" y="-60%" width="220%" height="220%">
                    <feGaussianBlur stdDeviation="7" result="coloredBlur"/>
                    <feMerge>
                      <feMergeNode in="coloredBlur"/>
                      <feMergeNode in="SourceGraphic"/>
                    </feMerge>
                  </filter>

                  <!-- Track outer glow filter (depth layer) -->
                  <filter id="track-glow" x="-20%" y="-200%" width="140%" height="500%">
                    <feGaussianBlur stdDeviation="3" result="glow"/>
                    <feMerge>
                      <feMergeNode in="glow"/>
                      <feMergeNode in="SourceGraphic"/>
                    </feMerge>
                  </filter>

                  <!-- Linear gradient for the active glowing track -->
                  <linearGradient id="track-flow-gradient" x1="0%" y1="0%" x2="100%" y2="100%">
                    <stop offset="0%" style="stop-color: var(--primary)" stop-opacity="0.95" />
                    <stop offset="50%" style="stop-color: var(--secondary)" stop-opacity="0.9" />
                    <stop offset="100%" style="stop-color: var(--primary)" stop-opacity="0.95" />
                  </linearGradient>

                  <!-- Glow version of track gradient -->
                  <linearGradient id="track-glow-gradient" x1="0%" y1="0%" x2="100%" y2="100%">
                    <stop offset="0%" style="stop-color: var(--primary)" stop-opacity="0.25" />
                    <stop offset="50%" style="stop-color: var(--secondary)" stop-opacity="0.2" />
                    <stop offset="100%" style="stop-color: var(--primary)" stop-opacity="0.25" />
                  </linearGradient>

                  <!-- Primary signal particle gradient (white-hot center) -->
                  <radialGradient id="signal-particle-fill" cx="50%" cy="35%" r="65%">
                    <stop offset="0%" style="stop-color: var(--text-primary)" />
                    <stop offset="35%" style="stop-color: var(--text-primary)" />
                    <stop offset="100%" style="stop-color: var(--primary)" stop-opacity="0.6" />
                  </radialGradient>

                  <!-- Trailing particle gradient (amber/secondary) -->
                  <radialGradient id="trail-particle-fill" cx="50%" cy="35%" r="65%">
                    <stop offset="0%" style="stop-color: var(--text-primary)" />
                    <stop offset="100%" style="stop-color: var(--secondary)" stop-opacity="0.5" />
                  </radialGradient>

                  <!-- Mint micro trail -->
                  <radialGradient id="mint-particle-fill" cx="50%" cy="35%" r="65%">
                    <stop offset="0%" style="stop-color: var(--text-primary)" />
                    <stop offset="100%" style="stop-color: var(--success)" stop-opacity="0.5" />
                  </radialGradient>
                </defs>

                <!-- Outermost ghost track (deepest depth layer, thick blur) -->
                <path
                  d={desktopCircuitPath}
                  stroke="url(#track-glow-gradient)"
                  stroke-width="10"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  opacity="0.45"
                  filter="url(#track-glow)"
                  class="dark:opacity-[0.55]"
                />

                <!-- Mid depth track (solid base) -->
                <path
                  d={desktopCircuitPath}
                  class="stroke-slate-300/40 dark:stroke-white/[0.06]"
                  stroke-width="2.5"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                />

                <!-- Active gradient track (luminous core) -->
                <path
                  d={desktopCircuitPath}
                  stroke="url(#track-flow-gradient)"
                  stroke-width="1.5"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  opacity="0.75"
                  class="dark:opacity-[0.9]"
                />

                <!-- Flowing dashed pulse (marching ants effect) -->
                <path
                  d={desktopCircuitPath}
                  stroke="url(#track-flow-gradient)"
                  stroke-width="1.5"
                  stroke-dasharray="6 14"
                  class="pipeline-circuit-flow"
                  stroke-linecap="round"
                  stroke-linejoin="round"
                />

                <!-- Primary luminous data signal (lead particle) -->
                <circle
                  r="6"
                  fill="url(#signal-particle-fill)"
                  filter="url(#signal-glow)"
                  class="pipeline-signal-particle"
                >
                  <animateMotion
                    dur="10s"
                    repeatCount="indefinite"
                    path={desktopCircuitPath}
                    rotate="auto"
                  />
                </circle>

                <!-- Secondary trailing packet (violet) -->
                <circle
                  r="4"
                  fill="url(#trail-particle-fill)"
                  opacity="0.8"
                  filter="url(#signal-glow)"
                  class="pipeline-signal-particle"
                >
                  <animateMotion
                    dur="10s"
                    begin="-0.55s"
                    repeatCount="indefinite"
                    path={desktopCircuitPath}
                    rotate="auto"
                  />
                </circle>

                <!-- Tertiary micro-packet (mint) -->
                <circle
                  r="2.5"
                  fill="url(#mint-particle-fill)"
                  opacity="0.65"
                  filter="url(#signal-glow)"
                  class="pipeline-signal-particle"
                >
                  <animateMotion
                    dur="10s"
                    begin="-1.1s"
                    repeatCount="indefinite"
                    path={desktopCircuitPath}
                    rotate="auto"
                  />
                </circle>
              </svg>

              <!-- =================================================================== -->
              <!-- 7 SEQUENTIALLY POSITIONED & ANIMATED DEVOPS STAGE CAPSULES          -->
              <!-- =================================================================== -->
              {pipelineStages.map((stage) => (
                <div
                  class="pipeline-node-wrapper absolute z-10 -translate-x-1/2 -translate-y-1/2 group cursor-pointer"
                  style={`left: ${(stage.x / 640) * 100}%; top: ${(stage.y / 480) * 100}%;`}
                  data-stage-id={stage.stage}
                  data-accent={stage.accent}
                  tabindex="0"
                  role="button"
                  aria-label={`Stage ${stage.stage}: ${stage.name} (${stage.tag}) - ${stage.highlight}`}
                >
                  <!-- Hover ambient glow halo (behind capsule) -->
                  <div
                    class="pipeline-node-glow absolute -inset-4 rounded-3xl opacity-0 group-hover:opacity-100 transition-opacity duration-500 blur-2xl pointer-events-none"
                    style={`background: radial-gradient(ellipse 80% 80%, ${stage.accentGlow} 0%, transparent 70%);`}
                  ></div>

                  <!-- Sequential activation pulse halo (synced with signal particle) -->
                  <div
                    class="pipeline-node-pulse-ring absolute -inset-2 rounded-3xl pointer-events-none"
                    style={`border: 1.5px solid ${stage.accentColor}; animation-delay: ${stage.delay};`}
                  ></div>

                  <!-- Stage Interactive Capsule -->
                  <div
                    class:list={[
                      "pipeline-stage-capsule relative flex flex-col gap-1.5 rounded-2xl p-2.5 sm:p-3",
                      "transition-all duration-300 backdrop-blur-md",
                      "min-w-[145px] max-w-[170px]",
                      stage.stage === '07' && "min-w-[280px] max-w-[340px]"
                    ]}
                    style={`--stage-accent: ${stage.accentColor}; --stage-glow: ${stage.accentGlow}; --stage-border: ${stage.accentBorder}; animation-delay: ${stage.delay};`}
                  >
                    <!-- Stage Header: Stage Number Badge + Category Tag -->
                    <div class="flex items-center justify-between gap-1">
                      <span class:list={[
                        "font-mono text-[9.5px] font-extrabold uppercase px-1.5 py-0.5 rounded-md",
                        stage.accent === 'cyan' && "bg-cyan/10 text-cyan dark:text-cyan",
                        stage.accent === 'violet' && "bg-violet/10 text-violet dark:text-violet",
                        stage.accent === 'mint' && "bg-mint/10 text-mint dark:text-mint"
                      ]}>
                        {stage.stage} · {stage.tag}
                      </span>

                      <span class="font-mono text-[8px] text-slate-500 dark:text-slate-400 uppercase tracking-tight truncate">
                        {stage.shortName}
                      </span>
                    </div>

                    <!-- Stage Technology Logos + Names -->
                    <div class="flex items-center flex-wrap gap-2 pt-0.5">
                      {stage.technologies.map((t) => (
                        <div class="flex items-center gap-1.5">
                          <span class="flex-shrink-0 transition-transform duration-200 group-hover:scale-110">
                            <TechIcon name={t.id} size={18} />
                          </span>
                          <span class="font-display font-bold text-xs text-slate-900 dark:text-white leading-tight">
                            {t.name}
                          </span>
                        </div>
                      ))}
                    </div>

                    <!-- Micro Capability Caption -->
                    <p class="text-[9.5px] leading-tight text-slate-500 dark:text-slate-400 truncate mt-0.5">
                      {stage.highlight}
                    </p>
                  </div>
                </div>
              ))}

            </div>

            <!-- Bottom Telemetry Status Bar -->
            <div class="mt-2 pt-3 border-t border-slate-200/70 dark:border-white/[0.06] flex items-center justify-between text-xs font-mono">
              <div class="flex items-center gap-1 text-[10px] text-slate-500 dark:text-slate-400">
                <span class="font-bold text-slate-700 dark:text-slate-300">FLOW:</span>
                <span>Cloud</span>
                <span class="text-slate-400 dark:text-slate-500">→</span>
                <span>IaC</span>
                <span class="text-slate-400 dark:text-slate-500">→</span>
                <span>OCI</span>
                <span class="text-slate-400 dark:text-slate-500">→</span>
                <span>K8s</span>
                <span class="text-slate-400 dark:text-slate-500">→</span>
                <span>CI/CD</span>
                <span class="text-slate-400 dark:text-slate-500">→</span>
                <span>GitOps</span>
                <span class="text-slate-400 dark:text-slate-500">→</span>
                <span class="text-mint font-bold">Observe</span>
              </div>

              <div class="flex items-center gap-1.5 text-[10px] text-mint">
                <span class="h-1.5 w-1.5 rounded-full bg-mint animate-pulse"></span>
                <span class="font-bold">99.9% Availability</span>
              </div>
            </div>
          </div>

        </div>

      </div>

    </div>

    <!-- ========================================================================= -->
    <!-- MOBILE / TABLET VERTICAL DEVOPS PIPELINE (< 1024px)                       -->
    <!-- ========================================================================= -->
    <div class="block lg:hidden mt-8 sm:mt-10">

      <div class="rounded-3xl border border-slate-200/80 dark:border-white/[0.07] bg-white/85 dark:bg-panel/90 backdrop-blur-xl p-4 sm:p-6 space-y-4 shadow-2xl overflow-hidden">

        <!-- Mobile Pipeline Header -->
        <div class="flex items-center justify-between pb-3 border-b border-slate-200/70 dark:border-white/[0.06]">
          <div class="flex items-center gap-2">
            <span class="relative flex h-2 w-2">
              <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-cyan opacity-75"></span>
              <span class="relative inline-flex rounded-full h-2 w-2 bg-cyan"></span>
            </span>
            <span class="font-mono text-xs font-bold uppercase tracking-wider text-slate-900 dark:text-white">
              DevOps Delivery Flow
            </span>
          </div>

          <span class="font-mono text-[10px] text-cyan font-bold bg-cyan/10 border border-cyan/20 px-2.5 py-0.5 rounded-full flex items-center gap-1">
            <span class="h-1.5 w-1.5 rounded-full bg-mint animate-pulse"></span>
            7 Active Stages
          </span>
        </div>

        <!-- Vertical Flow Container with Animated Spine Line -->
        <div class="relative pl-6 sm:pl-8 space-y-3 sm:space-y-4 mobile-pipeline-track">

          <!-- Vertical Pulsing Spine -->
          <div class="absolute left-2.5 sm:left-3.5 top-3 bottom-4 w-0.5 rounded-full overflow-hidden bg-slate-200 dark:bg-white/10" aria-hidden="true">
            <!-- Animated flowing light gradient beam traveling down spine -->
            <div class="mobile-spine-beam w-full h-28 bg-gradient-to-b from-transparent via-cyan to-transparent"></div>
          </div>

          <!-- 7 Vertical Stage Cards -->
          {pipelineStages.map((stage) => (
            <div class="relative flex items-start gap-3 mobile-stage-row" style={`--stage-delay: ${stage.delay}; --stage-accent: ${stage.accentColor}; --stage-glow: ${stage.accentGlow};`}>

              <!-- Node Anchor Dot on Vertical Spine -->
              <div class="absolute -left-6 sm:-left-8 top-3 flex items-center justify-center">
                <!-- Outer Pulse Ring -->
                <span class="mobile-anchor-pulse absolute -inset-1 rounded-full pointer-events-none" style={`border: 1.5px solid ${stage.accentColor}; animation-delay: ${stage.delay};`}></span>

                <span class:list={[
                  "relative h-3.5 w-3.5 rounded-full border-2 border-white dark:border-panel shadow-sm flex items-center justify-center transition-colors duration-300",
                  stage.accent === 'cyan' && "bg-cyan",
                  stage.accent === 'violet' && "bg-violet",
                  stage.accent === 'mint' && "bg-mint"
                ]}>
                  <span class="h-1 w-1 rounded-full bg-white"></span>
                </span>
              </div>

              <!-- Stage Content Card -->
              <div class="mobile-stage-card flex-1 rounded-2xl border border-slate-200/70 dark:border-white/[0.06] bg-slate-50/90 dark:bg-white/[0.03] p-3 sm:p-3.5 space-y-2 backdrop-blur-sm transition-all duration-300" style={`animation-delay: ${stage.delay};`}>
                <div class="flex items-center justify-between gap-1">
                  <span class:list={[
                    "font-mono text-[9px] font-extrabold uppercase px-1.5 py-0.5 rounded",
                    stage.accent === 'cyan' && "bg-cyan/10 text-cyan",
                    stage.accent === 'violet' && "bg-violet/10 text-violet",
                    stage.accent === 'mint' && "bg-mint/10 text-mint"
                  ]}>
                    STAGE {stage.stage} · {stage.tag}
                  </span>
                  <span class="font-mono text-[9px] text-slate-500 dark:text-slate-400 truncate">
                    {stage.name}
                  </span>
                </div>

                <div class="flex items-center flex-wrap gap-2.5 pt-0.5">
                  {stage.technologies.map((t) => (
                    <div class="flex items-center gap-1.5">
                      <TechIcon name={t.id} size={18} />
                      <span class="font-display font-bold text-xs text-slate-900 dark:text-white">
                        {t.name}
                      </span>
                    </div>
                  ))}
                </div>

                <p class="text-[11px] text-slate-600 dark:text-slate-400 leading-tight">
                  {stage.highlight}
                </p>
              </div>

            </div>
          ))}

        </div>

      </div>

    </div>

  </div>
</section>

<style>
  /* ============================================================ */
  /* AMBIENT ORBS (background depth field)                        */
  /* ============================================================ */
  .hero-orb {
    filter: blur(120px);
    will-change: transform;
  }

  .hero-orb--cyan {
    background: radial-gradient(circle, var(--primary-glow) 0%, transparent 70%);
  }

  :global(.dark) .hero-orb--cyan {
    background: radial-gradient(circle, var(--primary-glow) 0%, transparent 70%);
  }

  .hero-orb--violet {
    background: radial-gradient(circle, var(--secondary-glow) 0%, transparent 70%);
  }

  :global(.dark) .hero-orb--violet {
    background: radial-gradient(circle, var(--secondary-glow) 0%, transparent 70%);
  }

  .hero-orb--mint {
    background: radial-gradient(circle, var(--success-glow) 0%, transparent 70%);
  }

  :global(.dark) .hero-orb--mint {
    background: radial-gradient(circle, var(--success-glow) 0%, transparent 70%);
  }

  /* ============================================================ */
  /* ENGINEERING GRID PATTERN                                     */
  /* ============================================================ */
  .hero-grid-pattern {
    background-image:
      linear-gradient(var(--border) 1px, transparent 1px),
      linear-gradient(90deg, var(--border) 1px, transparent 1px);
    background-size: 48px 48px;
    mask-image: linear-gradient(to bottom, rgba(0,0,0,0.8), transparent 85%);
  }

  :global(.dark) .hero-grid-pattern {
    background-image:
      linear-gradient(var(--border) 1px, transparent 1px),
      linear-gradient(90deg, var(--border) 1px, transparent 1px);
  }

  /* ============================================================ */
  /* MICRO DOT GRID (inside console panel)                        */
  /* ============================================================ */
  .hero-dot-grid {
    background-image: radial-gradient(circle, var(--border) 1px, transparent 1px);
    background-size: 20px 20px;
  }

  :global(.dark) .hero-dot-grid {
    background-image: radial-gradient(circle, var(--border) 1px, transparent 1px);
  }

  /* ============================================================ */
  /* HERO ENTRANCE ANIMATION                                       */
  /* ============================================================ */
  @keyframes heroFadeIn {
    from {
      opacity: 0;
      transform: translateY(18px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .hero-animate-in {
    animation: heroFadeIn 0.75s cubic-bezier(0.16, 1, 0.3, 1) both;
  }

  /* ============================================================ */
  /* NAME GRADIENT                                                 */
  /* ============================================================ */
  .hero-name-gradient {
    background: linear-gradient(135deg, var(--primary) 0%, var(--text-primary) 50%, var(--secondary) 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  /* ============================================================ */
  /* PROOF PILLS                                                   */
  /* ============================================================ */
  .hero-proof-pill {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    padding: 5px 10px;
    border-radius: 8px;
    border: 1px solid var(--border);
    background: var(--surface);
    color: var(--text-secondary);
    font-size: 11px;
    transition: border-color 0.2s, background 0.2s;
  }

  :global(.dark) .hero-proof-pill {
    border-color: var(--border);
    background: var(--surface);
    color: var(--text-secondary);
  }

  .hero-proof-pill:hover {
    border-color: var(--primary);
    background: var(--surface-elevated);
  }

  /* ============================================================ */
  /* CTA BUTTONS                                                   */
  /* ============================================================ */
  .hero-cta-primary {
    border-radius: 12px;
    background: var(--primary);
    color: var(--background);
    padding: 14px 24px;
    font-size: 0.9rem;
    font-weight: 700;
    box-shadow: 0 4px 14px var(--primary-glow);
    transition: transform 0.15s ease, box-shadow 0.15s ease, background 0.15s ease;
  }

  .hero-cta-primary:hover {
    background: var(--primary);
    transform: translateY(-1px) scale(1.015);
    box-shadow: 0 8px 24px var(--primary-glow);
  }

  .hero-cta-primary:active {
    transform: scale(0.98);
  }

  :global(.dark) .hero-cta-primary {
    background: var(--primary);
    color: var(--background);
    box-shadow: 0 4px 14px var(--primary-glow);
  }

  :global(.dark) .hero-cta-primary:hover {
    box-shadow: 0 8px 28px var(--primary-glow);
  }

  .hero-cta-secondary {
    border-radius: 12px;
    border: 1px solid var(--border);
    background: var(--surface);
    color: var(--text-primary);
    padding: 14px 20px;
    font-size: 0.9rem;
    font-weight: 600;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    transition: transform 0.15s ease, border-color 0.2s, background 0.2s, box-shadow 0.2s;
  }

  .hero-cta-secondary:hover {
    border-color: var(--primary);
    background: var(--surface-elevated);
    transform: translateY(-1px);
    box-shadow: 0 4px 12px var(--primary-glow);
  }

  .hero-cta-secondary:active {
    transform: scale(0.98);
  }

  :global(.dark) .hero-cta-secondary {
    border-color: var(--border);
    background: var(--surface);
    color: var(--text-primary);
    box-shadow: none;
  }

  :global(.dark) .hero-cta-secondary:hover {
    border-color: var(--primary);
    background: var(--surface-elevated);
  }

  /* ============================================================ */
  /* PIPELINE CONSOLE PANEL                                        */
  /* ============================================================ */
  .pipeline-console-panel {
    box-shadow:
      0 30px 80px rgba(0, 0, 0, 0.15),
      0 10px 24px rgba(0, 0, 0, 0.08),
      0 0 0 1px var(--border);
  }

  :global(.dark) .pipeline-console-panel {
    box-shadow:
      0 40px 100px rgba(0, 0, 0, 0.5),
      0 12px 40px rgba(0, 0, 0, 0.35),
      0 0 0 1px var(--border);
  }

  /* ============================================================ */
  /* PROFILE PHOTO HALO (always-visible subtle breathing glow)    */
  /* ============================================================ */
  @keyframes profileHaloPulse {
    0%, 100% {
      opacity: 0.35;
      transform: scale(1);
    }
    50% {
      opacity: 0.7;
      transform: scale(1.05);
    }
  }

  .hero-profile-halo {
    background: radial-gradient(ellipse, var(--primary-glow) 0%, var(--secondary-glow) 45%, transparent 70%);
    animation: profileHaloPulse 3.5s ease-in-out infinite;
  }

  :global(.dark) .hero-profile-halo {
    background: radial-gradient(ellipse, var(--primary-glow) 0%, var(--secondary-glow) 45%, transparent 70%);
  }

  /* ============================================================ */
  /* CIRCUIT TRACK FLOW ANIMATION (marching ants)                 */
  /* ============================================================ */
  @keyframes circuitTrackFlow {
    to {
      stroke-dashoffset: -40;
    }
  }

  .pipeline-circuit-flow {
    opacity: 0.6;
    animation: circuitTrackFlow 2.2s linear infinite;
  }

  :global(.dark) .pipeline-circuit-flow {
    opacity: 0.85;
  }

  /* ============================================================ */
  /* STAGE CAPSULE: BASE + HOVER                                   */
  /* ============================================================ */
  .pipeline-stage-capsule {
    background: var(--surface);
    border: 1.5px solid var(--border);
    box-shadow:
      0 4px 14px rgba(0, 0, 0, 0.08),
      0 1px 3px rgba(0, 0, 0, 0.05);
    animation: stageSequentialPulse 10s ease-in-out infinite;
    will-change: border-color, box-shadow, transform;
    transition: transform 0.25s cubic-bezier(0.34, 1.56, 0.64, 1), box-shadow 0.25s ease;
  }

  :global(.dark) .pipeline-stage-capsule {
    background: var(--surface);
    border: 1.5px solid var(--border);
    box-shadow:
      0 4px 20px rgba(0, 0, 0, 0.35),
      0 1px 4px rgba(0, 0, 0, 0.2);
    animation: stageSequentialPulseDark 10s ease-in-out infinite;
  }

  /* Hover state for stage capsules */
  .pipeline-node-wrapper:hover .pipeline-stage-capsule,
  .pipeline-node-wrapper:focus .pipeline-stage-capsule {
    transform: scale(1.07) translateY(-2px);
    box-shadow:
      0 16px 40px var(--stage-glow),
      0 4px 14px rgba(0, 0, 0, 0.15),
      0 0 0 1.5px var(--stage-accent);
    border-color: var(--stage-accent) !important;
  }

  :global(.dark) .pipeline-node-wrapper:hover .pipeline-stage-capsule,
  :global(.dark) .pipeline-node-wrapper:focus .pipeline-stage-capsule {
    box-shadow:
      0 16px 40px var(--stage-glow),
      0 4px 14px rgba(0, 0, 0, 0.3),
      0 0 0 1.5px var(--stage-accent);
  }

  /* ============================================================ */
  /* SEQUENTIAL PULSE (node activation synced with signal)        */
  /* ============================================================ */

  /* Light mode pulse */
  @keyframes stageSequentialPulse {
    0%, 18%, 100% {
      border-color: var(--border);
      box-shadow:
        0 4px 14px rgba(0, 0, 0, 0.08),
        0 1px 3px rgba(0, 0, 0, 0.05);
    }
    4%, 14% {
      border-color: var(--stage-accent);
      box-shadow:
        0 0 0 2px var(--stage-glow),
        0 0 24px var(--stage-glow),
        0 0 10px var(--stage-glow);
    }
    9% {
      border-color: var(--stage-accent);
      box-shadow:
        0 0 0 4px var(--stage-glow),
        0 0 36px var(--stage-glow),
        0 0 16px var(--stage-glow);
    }
  }

  /* Dark mode pulse */
  @keyframes stageSequentialPulseDark {
    0%, 18%, 100% {
      border-color: var(--border);
      box-shadow:
        0 4px 20px rgba(0, 0, 0, 0.35),
        0 1px 4px rgba(0, 0, 0, 0.2);
    }
    4%, 14% {
      border-color: var(--stage-accent);
      box-shadow:
        0 0 0 3px var(--stage-glow),
        0 0 35px var(--stage-glow),
        0 0 14px var(--stage-glow),
        0 8px 24px rgba(0, 0, 0, 0.3);
    }
    9% {
      border-color: var(--stage-accent);
      box-shadow:
        0 0 0 6px var(--stage-glow),
        0 0 60px var(--stage-glow),
        0 0 24px var(--stage-glow),
        0 8px 24px rgba(0, 0, 0, 0.3);
    }
  }

  /* ============================================================ */
  /* NODE PULSE RING (sequential activation ring around node)     */
  /* ============================================================ */
  @keyframes nodeActivationRing {
    0%, 18%, 100% {
      opacity: 0;
      transform: scale(0.9);
    }
    4% {
      opacity: 0.6;
      transform: scale(1);
    }
    9% {
      opacity: 0.8;
      transform: scale(1.02);
    }
    14% {
      opacity: 0.3;
      transform: scale(1.05);
    }
  }

  .pipeline-node-pulse-ring {
    animation: nodeActivationRing 10s ease-in-out infinite;
    border-radius: 1.25rem;
    opacity: 0;
  }

  /* ============================================================ */
  /* MOBILE PIPELINE ANIMATIONS                                   */
  /* ============================================================ */
  @keyframes mobileSpineFlow {
    0% {
      transform: translateY(-100%);
    }
    100% {
      transform: translateY(600%);
    }
  }

  .mobile-spine-beam {
    animation: mobileSpineFlow 8.4s linear infinite;
    will-change: transform;
  }

  @keyframes mobileCardActivate {
    0%, 18%, 100% {
      border-color: var(--border);
      box-shadow: none;
    }
    4%, 14% {
      border-color: var(--stage-accent);
      box-shadow: 0 0 16px var(--stage-glow), 0 0 0 1px var(--stage-accent);
    }
    9% {
      border-color: var(--stage-accent);
      box-shadow: 0 0 24px var(--stage-glow), 0 0 0 1.5px var(--stage-accent);
    }
  }

  .mobile-stage-card {
    animation: mobileCardActivate 8.4s ease-in-out infinite;
    will-change: border-color, box-shadow;
  }

  @keyframes mobileAnchorPulseRing {
    0%, 18%, 100% {
      opacity: 0;
      transform: scale(0.8);
    }
    4% {
      opacity: 0.8;
      transform: scale(1.3);
    }
    9% {
      opacity: 1;
      transform: scale(1.6);
    }
    14% {
      opacity: 0;
      transform: scale(1.9);
    }
  }

  .mobile-anchor-pulse {
    animation: mobileAnchorPulseRing 8.4s ease-out infinite;
  }

  /* ============================================================ */
  /* SIGNAL PARTICLE VISIBILITY                                    */
  /* ============================================================ */
  .pipeline-signal-particle {
    opacity: 1;
  }

  /* ============================================================ */
  /* ACCESSIBILITY: PREFERS REDUCED MOTION                         */
  /* ============================================================ */
  @media (prefers-reduced-motion: reduce) {
    .hero-animate-in {
      animation: none !important;
      opacity: 1 !important;
      transform: none !important;
    }

    .pipeline-circuit-flow {
      animation: none !important;
    }

    .pipeline-stage-capsule {
      animation: none !important;
    }

    .pipeline-node-pulse-ring {
      animation: none !important;
      opacity: 0 !important;
    }

    .pipeline-signal-particle {
      display: none !important;
    }

    .hero-profile-halo {
      animation: none !important;
    }

    .mobile-spine-beam {
      animation: none !important;
      display: none !important;
    }

    .mobile-stage-card {
      animation: none !important;
    }

    .mobile-anchor-pulse {
      animation: none !important;
      display: none !important;
    }
  }
</style>

<script is:inline>
  (function () {
    const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)');
    const container = document.getElementById('hero-venna-typing');
    if (!container) return;

    const letters = [
      { char: 'V', cls: 'venna-char-1' },
      { char: 'E', cls: 'venna-char-2' },
      { char: 'N', cls: 'venna-char-3' },
      { char: 'N', cls: 'venna-char-4' },
      { char: 'A', cls: 'venna-char-5' }
    ];

    function renderLetters(count) {
      container.innerHTML = letters
        .slice(0, count)
        .map(function (l) {
          return '<span class="venna-char ' + l.cls + '">' + l.char + '</span>';
        })
        .join('');
    }

    if (prefersReducedMotion.matches) {
      renderLetters(letters.length);
      return;
    }

    let currentIndex = letters.length;
    let isDeleting = true;
    let timer = null;

    function step() {
      if (isDeleting) {
        currentIndex--;
        renderLetters(currentIndex);

        if (currentIndex === 0) {
          isDeleting = false;
          timer = setTimeout(step, 450);
        } else {
          timer = setTimeout(step, 90);
        }
      } else {
        currentIndex++;
        renderLetters(currentIndex);

        if (currentIndex === letters.length) {
          isDeleting = true;
          timer = setTimeout(step, 2000);
        } else {
          timer = setTimeout(step, 140);
        }
      }
    }

    timer = setTimeout(step, 2200);

    prefersReducedMotion.addEventListener('change', function (e) {
      if (e.matches) {
        if (timer) clearTimeout(timer);
        renderLetters(letters.length);
      } else {
        currentIndex = letters.length;
        isDeleting = true;
        timer = setTimeout(step, 2000);
      }
    });
  })();
</script>
